import { ActivatedDirective } from './activated.directive';

describe('ActivatedDirective', () => {
  it('should create an instance', () => {
    const directive = new ActivatedDirective();
    expect(directive).toBeTruthy();
  });
});
